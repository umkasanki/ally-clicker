# AllyClicker — Specification

> Документ обновляется по мере поступления требований.
> Последнее обновление: 2026-06-07

---

## Назначение

AllyClicker — инструмент доступности для пользователей, управляющих курсором с помощью хедтрекера.
Заменяет физические кнопки мыши: пользователь наводит курсор на кнопку панели и удерживает его
(dwell-click) — приложение выполняет соответствующее действие мыши.

Аналог: Point-N-Click (polital.com/pnc), .NET Framework, устарел, нестабилен.

---

## Приложения в составе проекта

| Приложение | Описание |
|---|---|
| `panel` | Основная панель с кнопками кликов, всегда поверх окон |
| `configure` | Отдельное приложение настройки «Configure AllyClicker» |

---

## 1. Панель (panel)

### Внешний вид
- Вертикальная узкая панель, докирована к правому краю экрана
- Кнопки с иконками мыши (аналогично оригиналу)
- Всегда поверх всех окон (`topmost`)
- Не перехватывает фокус (`WS_EX_NOACTIVATE`)

### Кнопка 1 — ON/OFF (Активация / сворачивание)

- Иконка: символ питания (⏻)
- При включённом автозапуске панель стартует в **свёрнутом виде** — видна только эта кнопка
- Пользователь наводит курсор и удерживает `AutoMouse Delay` → панель **разворачивается**
- Направление разворота: **вправо** или **вниз** — настраивается в Options
- Повторный dwell на ON/OFF → панель **сворачивается** обратно

---

### Кнопка 2 — LEFT (Левый клик, ЛКМ)

- Выполняет одиночный клик левой кнопкой мыши
- Клик отправляется на **последнюю позицию курсора вне панели**
- Становится **текущей активной функцией** после выбора
- Если включена опция **Default to Left Click** — активна по умолчанию при старте и после сброса
- Используется `SendInput` (Win32 API) — уровень ОС, не зависит от состояния целевого приложения
- Выполнение — в отдельном потоке (панель не блокируется)

---

### Кнопка 3 — RIGHT (Правый клик, ПКМ)

- Выполняет одиночный клик правой кнопкой мыши
- Клик отправляется на **последнюю позицию курсора вне панели**
- Становится **текущей активной функцией** после выбора
- Подчиняется общей механике активной функции (Automatic Cancel, Default to Left Click)

---

### Кнопка 4 — DRAG (Drag / Выделение)

Универсальный drag: выделение текста, перемещение объектов, выделение области.
Один и тот же механизм — результат зависит от того над чем находится курсор в момент `mouse_down`:
- Над текстом → выделение текста
- Над файлом/иконкой → перемещение объекта
- Над пустой областью → выделение области (как в файловом менеджере)

1. Текущая активная функция = **Выделение**
2. Курсор останавливается в точке **①** на **AutoSelect Delay / Параметр 1** → `mouse_down` (ЛКМ зажата)
3. Пользователь перемещает курсор к точке **②** (ЛКМ остаётся зажатой)
4. Курсор останавливается в точке **②** на **AutoSelect Delay / Параметр 2** → `mouse_up` (ЛКМ отпущена)
5. Выделение выполнено

Использует **AutoSelect Delay** (отдельный таймер, не AutoMouse Delay).

---

### Кнопка 5 — DOUBLE (Двойной клик, 2×ЛКМ)

- Выполняет двойной клик левой кнопкой мыши
- Клик отправляется на **последнюю позицию курсора вне панели**
- Становится **текущей активной функцией** после выбора
- Подчиняется общей механике активной функции (Automatic Cancel, Default to Left Click)

---

### Кнопка 6 — MIDDLE (Клик колёсиком / Auto-Scroll)

- Выполняет клик средней кнопкой мыши (колёсиком)
- Клик отправляется на **последнюю позицию курсора вне панели**
- Становится **текущей активной функцией** после выбора
- Подчиняется общей механике активной функции (Automatic Cancel, Default to Left Click)

**Три сценария использования:**

1. **Открыть ссылку в новой вкладке** — клик колёсиком по ссылке в браузере
2. **Закрыть вкладку** — клик колёсиком по вкладке в браузере
3. **Auto-Scroll** — активирует режим прокрутки в приложениях с прокручиваемым контентом:
   - **Windows:** нативный Auto-Scroll (встроен в ОС) — устанавливается якорная точка, смещение курсора вверх/вниз → скролл, чем дальше от якоря — тем быстрее
   - **macOS:** собственная реализация через `CGScrollWheelEvent` (см. раздел 7)
   - Зигзагообразное движение курсора предотвращает срабатывание AutoMouse Delay
   - Dwell в любой точке → ЛКМ (если Default to Left Click активна) → выход из Auto-Scroll

---

### Кнопка 7 — KEYBOARD (Запуск клавиатуры)

- Запускает экранную клавиатуру. Не является «активной функцией» — разовое действие.
- **Цель настраивается** (три режима, выбор в Configure):
  1. **Accessibility Keyboard** — встроенная в macOS (Settings → Accessibility → Keyboard). Дефолт.
  2. **Keyboard Viewer** — стандартная виртуальная клавиатура macOS.
  3. **Стороннее приложение** — путь/bundle id выбирается пользователем.
- В модели: `Settings.commands.keyboard: KeyboardTarget`
  (`.accessibilityKeyboard` / `.keyboardViewer` / `.customApp(path:)`)

---

### Конфигурируемый состав и порядок панели

Набор кнопок и их **порядок** настраиваются пользователем — `Settings.panel.items`
это упорядоченный список `PanelItem` (клик-действие или команда). Добавление/удаление
кнопки = вставка/удаление элемента; перетаскивание = смена порядка.
Таблица ниже — **дефолтная раскладка**, не фиксированная.

### Сводная таблица кнопок (дефолтная раскладка)

> Подтверждено по скриншоту реального PNC. Порядок сверху вниз.

| # | ID | Действие | Таймер | Активная функция | Описание |
|---|---|---|---|---|---|
| 1 | `ON/OFF` | Развернуть / свернуть панель | AutoMouse Delay | — | Управление видимостью панели. Всегда сверху |
| 2 | `LEFT` | Левый клик (ЛКМ) | AutoMouse Delay | ✓ | Одиночный клик левой кнопкой. По умолчанию **armed** (красный) |
| 3 | `RIGHT` | Правый клик (ПКМ) | AutoMouse Delay | ✓ | Одиночный клик правой кнопкой |
| 4 | `DOUBLE` | Двойной клик (2×ЛКМ) | AutoMouse Delay | ✓ | Двойной клик левой кнопкой |
| 5 | `DRAG` | Drag / Выделение | AutoSelect Delay (×2) | ✓ | mouse_down → move → mouse_up. Выделение текста, перемещение объектов |
| 6 | `MIDDLE` | Клик колёсиком | AutoMouse Delay | ✓ | Клик средней кнопкой мыши |
| 7 | `KEYBOARD` | Запуск клавиатуры | AutoMouse Delay | — | Запускает настраиваемую клавиатуру (3 режима). Серый фон — не активная функция |

---

## 5. Текущая активная функция (Current Active Function)

Пользователь выбирает функцию на панели → она становится **текущей активной**.
Пока функция активна: остановка курсора в **любой точке экрана** (вне панели) на `AutoMouse Delay` → выполняется действие.
Активная кнопка на панели визуально выделяется.

---

### Сброс текущей активной функции

Сброс происходит при **наведении или пересечении области панели** (достаточно быстрого движения в сторону панели — «мотнул головой»).
После сброса курсор перестаёт кликать при остановках.

Также сброс/смена происходит при выборе другой функции на панели.

---

### Опция: Automatic Cancel

| Состояние | Поведение |
|---|---|
| **Вкл** | После **однократного** выполнения действия → автоматический сброс текущей активной функции |
| **Выкл** | Функция повторяется при каждой остановке курсора на `AutoMouse Delay` по всему экрану, пока не будет сброшена вручную (наведением на панель) или заменена другой |

---

### Опция: Default to Left Click

| Состояние | Поведение |
|---|---|
| **Вкл** | При старте приложения — активна **ЛКМ**. После выполнения любой другой функции (в т.ч. с Automatic Cancel) — активная функция **переключается на ЛКМ** (не полный сброс, а замена на левый клик) |
| **Выкл** | После сброса — нет активной функции; клики по экрану не выполняются |

---

### Взаимодействие опций — сводная таблица

| Default to Left Click | Automatic Cancel | После выполнения действия |
|---|---|---|
| Выкл | Выкл | Функция повторяется бесконечно |
| Выкл | Вкл | Сброс после первого выполнения |
| Вкл | Выкл | Функция повторяется бесконечно (ЛКМ остаётся активной) |
| Вкл | Вкл | После первого выполнения → переключение на ЛКМ |

---

## 2. Dwell-click

- Механика: курсор входит в кнопку → запускается таймер → по истечении выполняется действие
- Курсор ушёл до истечения → таймер отменяется
- **Визуальная индикация отсчёта (кружок-индикатор) — НЕ реализуется** (по предпочтению
  пользователя; привычка работать без неё). Движок отдаёт эффект `.dwellProgress` для
  полноты, но панель его намеренно не рисует. Состояние «выбрана» (armed) — рисуется
  (красная кнопка), отсчёт — нет. Фичу можно включить позже без изменения движка.
- Задержка настраивается через **AutoMouse Delay** (см. меню Options)

### AutoMouse Delay
- Применяется ко **всем кнопкам панели**, кроме кнопки выделения (для неё — AutoSelect Delay)
- Включает разворачивание панели (кнопка ON/OFF)
- Точность: **0.1 секунды** (например: 0.20с, 0.35с, 0.19с)
- Виджет ввода: поле с кнопками ◄ ► для пошагового изменения + ручной ввод + Ok / Cancel

### AutoSelect Delay
- Применяется только к кнопке **Выделения (Selection)** на панели
- Содержит **два отдельных интервала**, настраиваемых одним диалогом:
  - **Параметр 1** — время удержания курсора в точке ① → `mouse_down` (зажать ЛКМ)
  - **Параметр 2** — время удержания курсора в точке ② → `mouse_up` (отпустить ЛКМ)
- Точность: 0.1с. Виджет: поле + кнопки ◄ ► + Ok/Cancel

### Механика кнопки Selection (Выделение)
Симулирует выделение мышью (drag с зажатой ЛКМ):

1. Текущая активная функция = **Выделение**
2. Курсор останавливается в точке **①** на **Параметр 1** → `mouse_down` (ЛКМ зажата)
3. Пользователь перемещает курсор к точке **②** (ЛКМ остаётся зажатой)
4. Курсор останавливается в точке **②** на **Параметр 2** → `mouse_up` (ЛКМ отпущена)
5. Выделение выполнено

---

## 3. Приложение настройки (configure)

### Внешний вид
- Отдельное окно «Configure AllyClicker»
- Меню-бар: **File | Options | Selections | Help**

### Меню File
| Пункт | Тип | Описание |
|---|---|---|
| Exit | action | Закрыть приложение настройки |

### Меню Options
| Пункт | Тип | Описание |
|---|---|---|
| Run at Boot Up | action | — *описание будет добавлено* |
| Default to Left Click | checkbox | При старте и после сброса — активной функцией автоматически становится ЛКМ |
| Automatic Cancel | checkbox | Если вкл — сброс активной функции после однократного выполнения. Если выкл — функция повторяется при каждой остановке курсора |
| Visible When Active | checkbox | — *описание будет добавлено* |
| Change Form Size... | dialog | — *описание будет добавлено* |
| Single Row Form | checkbox | — *описание будет добавлено* |
| Audio Dwelling Feedback | checkbox | — *описание будет добавлено* |
| AutoSelect Delay... | dialog | Время удержания для кнопки выделения. Точность 0.1с. Виджет: поле + кнопки ◄ ► + Ok/Cancel |
| AutoMouse Delay... | dialog | Время удержания курсора перед выполнением действия. Точность 0.1с. Виджет: поле + кнопки ◄ ► + Ok/Cancel |
| Sensitivity... | dialog | Высокоуровневый слайдер чувствительности. Управляет двумя параметрами одновременно: `dwell_radius` (допуск дрожания при удержании, дефолт 2px) и `move_radius` (минимальное смещение чтобы считать что мышь двинулась, дефолт 10px) |
| Transparency... | dialog | — *описание будет добавлено* |

### Меню Selections
| Пункт | Тип | Описание |
|---|---|---|
| AutoMouse Functions... | dialog | — *описание будет добавлено* |
| Color Selections... | dialog | — *описание будет добавлено* |

### Меню Help
| Пункт | Тип | Описание |
|---|---|---|
| About AllyClicker | dialog | Версия, описание приложения |

---

## 4. Технологии

| Компонент | Технология |
|---|---|
| Язык | Swift |
| Панель | AppKit — `NSPanel` (`.nonactivatingPanel`, высокий `window.level`) |
| Settings UI | AppKit / SwiftUI (Settings window) |
| Чтение позиции | `NSEvent.mouseLocation` / `CGEvent`, polling каждые `trackerIntervalMs` (5мс) |
| Инъекция кликов | `CGEvent.post(tap: .cgSessionEventTap)` — требует Accessibility permission |
| Конфиг | JSON (`Codable`), хранится в `~/Library/Application Support/AllyClicker/` |
| Платформа | macOS (Apple Silicon + Intel), macOS 14+ |

### Разрешения

Приложение требует **Accessibility access** (System Settings → Privacy & Security → Accessibility).
Без него `CGEvent.post` молча не работает. При запуске — проверка через `AXIsProcessTrustedWithOptions`,
при отсутствии — алерт с инструкцией как выдать разрешение.

---

## 6. Sensitivity — чувствительность

Высокоуровневый слайдер в Configure → Options → Sensitivity.
Управляет двумя низкоуровневыми параметрами одновременно:

| Параметр | Дефолт | Описание |
|---|---|---|
| `dwell_radius` | 2px | Радиус допуска дрожания при удержании. Курсор считается «остановившимся» пока не вышел за этот радиус |
| `move_radius` | 10px | Минимальное смещение чтобы считать что мышь **двинулась** (сброс таймера dwell) |

Важно для хедтрекера: голова всегда немного дрожит, без jitter radius dwell не будет срабатывать.

Слайдер Sensitivity меняет оба параметра пропорционально — пользователь не видит пиксели, только уровень чувствительности.

---

## 7. Auto-Scroll на macOS

На macOS нет нативного Auto-Scroll по клику колёсика (это Windows-механизм).
Реализуется вручную через `CGScrollWheelEvent`:

1. Фиксируется якорная точка `(x0, y0)` в момент `middle_click`
2. Dead zone 10px — движение меньше 10px от якоря игнорируется
3. Фоновый таймер (60 FPS) отслеживает смещение курсора от якоря
4. Генерируется `CGScrollWheelEvent` с дельтой, нелинейной от смещения:

   ```swift
   let event = CGEvent(scrollWheelEvent2Source: nil,
                       units: .pixel, wheelCount: 2,
                       wheel1: 0, wheel2: 0, wheel3: 0)
   event?.setDoubleValueField(.scrollWheelEventPointDeltaAxis1, value: vertical)
   event?.setDoubleValueField(.scrollWheelEventPointDeltaAxis2, value: horizontal)
   event?.post(tap: .cgSessionEventTap)
   ```

5. Зигзагообразное движение курсора — скролл продолжается (курсор не останавливается → AutoMouse Delay не срабатывает)
6. `left_click` (dwell в любой точке) → выход из Auto-Scroll режима

**Источник алгоритма:** [LinearMouse AutoScrollTransformer](https://github.com/linearmouse/linearmouse) (MIT).
Нелинейная скорость: `base + sqrt(adjusted) * boost`, макс 160px/тик.
Поля события:
- `.scrollWheelEventPointDeltaAxis1` — вертикальный скролл (Y)
- `.scrollWheelEventPointDeltaAxis2` — горизонтальный скролл (X)

---

## Открытые вопросы

- [ ] Визуальная индикация dwell-click на кнопке
- [ ] Время задержки dwell по умолчанию
- [ ] Механика Drag (состояния, отмена)
- [x] Как запускается основная панель — отдельно (`ally-clicker.exe`)
- [ ] Формат хранения настроек (ini, json, registry)
- [ ] Macros — не реализуются в текущей версии


---

## Дополнение по переписке с автором PNC (подтверждено 2026-06-29)

> Источник: письмо Anne York (автор Point-N-Click) с ответами на 8 вопросов.
> Дополняет и местами корректирует разделы выше (spec датирован 2026-06-07 — до переписки).

### Главный вывод — dwell над экраном ВЫЧИСЛЯЕТСЯ (адаптивная задержка)

В PNC задержка авто-клика над рабочим столом (`DwellTimeMouse`, у Олега ≈195 мс / 0.2 с)
**не задаётся вручную**, а рассчитывается из калибровки под конкретного пользователя:

```
DwellTimeMouse = Int(DwellMultiplier * Sensitivity_Twips / AverageVelocity)
```

- `AverageVelocity` — средняя скорость движения курсора, **замеряется в обязательном
  первичном калибровочном тесте** (baseline test). Медленнее движения → больше задержка.
- `Sensitivity_Twips` — чувствительность в твипах (легаси-единица Windows, 1/1440 дюйма).
- `DwellMultiplier` — общий множитель отзывчивости.
- `Int(...)` — округление до целых миллисекунд.

`BaselineFlags` — флаг «калибровка пройдена». Пока не пройдена, PNC не даёт собой
пользоваться: тест запускается принудительно при старте.

**Что это значит для AllyClicker (дополняет §2 «AutoMouse Delay» и закрывает пункт
«Открытые вопросы → Время задержки dwell по умолчанию»):**
Сейчас `AutoMouse Delay` — ручная фиксированная задержка, и ядро хранит
`dwellTimeMouseSeconds` как настройку. Это рабочий и достаточный вариант для личного
использования (Олег знает своё значение ≈0.2 с) — оставляем его как fallback. Но теперь
понятно, откуда в PNC берётся «подстройка под человека». **Открытое проектное решение:**
оставить ручную задержку как сейчас — или добавить опциональный калибровочный тест, который
один раз замеряет `averageVelocity` и вычисляет задержку по формуле (твипы заменить на
точки/пиксели macOS и заново подобрать `DwellMultiplier`, чтобы для скорости Олега выйти на
привычные ≈0.2 с). Формула — чистая арифметика, её место в ядре (движке), рядом с
dwell-логикой; калибровка потянет отдельный экран в Configure и, при желании, гейт на старте
(аналог `BaselineFlags`).

### Уточнение AutoCancel OFF (корректирует §5)

Автор уточнила: при **выключенном** Automatic Cancel выбранная кнопка остаётся активной,
пока пользователь не выберет другую кнопку **или не нажмёт кнопку Cancel**. Отмены
«мазком по панели» (swipe/brush) в этом режиме в PNC **нет**.

→ AllyClicker здесь **осознанно отходит от PNC**: у нас swipe-reset (сброс при заходе
курсора в зону панели) работает всегда, независимо от AutoCancel — это удобнее и уже
реализовано в ядре. Фиксируем как намеренное улучшение, а не расхождение-баг. (Если
когда-нибудь понадобится 1:1 как в PNC — добавить отдельную кнопку Cancel и отключать
swipe при AutoCancel off.)

### RAMB — Remote Access Mouse Button (в проекте не было)

RAMB — не «повтор». Это режим для случаев, когда полноэкранное приложение не даёт панели
быть поверх: пользователь задаёт на экране фиксированную точку-«якорь», наведясь на которую
можно зарядить функцию мыши, не доставая обычную панель. `RAMBTransparency` — прозрачность
этой точки.
→ macOS-аналог: маленькое floating-окно с уровнем выше fullscreen-Space
(`collectionBehavior` = `.canJoinAllSpaces` / `.fullScreenAuxiliary`) + Accessibility.
Сложнее обычной панели — **кандидат в Фазу 5**, после базовой панели.

### Уточнения действий кликов (дополняет §1 и сводную таблицу)

- `Left` / `Middle` = одиночный клик; `Left2` / `Middle2` = **двойной** левый / средний
  (наш DOUBLE соответствует `Left2` — всё сходится).
- `RightLeft` = **right-затем-left**: правый клик (открыть контекстное меню) и сразу левый
  (выбрать пункт). У Олега выключено, но это удобный кандидат в дополнительный `PanelItem`
  (см. §«Конфигурируемый состав панели»).

### Прочее

- `SensitivityV2` подтверждён как **допуск движения (радиус)** — совпадает с нашим
  `dwell_radius` (§6). Нюанс: в PNC чувствительность участвует **дважды** — и как радиус
  допуска, и как `Sensitivity_Twips` в формуле dwell. Если добавим калибровку — слайдер
  Sensitivity должен влиять и на задержку, не только на радиусы.
- `UseTimer` / `UseTimerReset` = интервалы **break-таймера** (напоминание об отдыхе).
  У нас не реализовано — опциональная фича на будущее.

### Ответы автора (запись, 8 пунктов)

1. Тайминги — в мс, в UI показываются в секундах. ✓
2. Детект неподвижности + `SensitivityV2` = допуск движения (радиус). ✓
3. Механика «armed → fire → revert» и swipe в PNC + поведение AutoCancel (см. выше). ✓
4. `UseTimer` / `UseTimerReset` = интервалы break-таймера. ✓
5. `BaselineFlags` = флаг калибровки; `AverageVelocity` из теста → формула dwell (см. выше). ✓
6. `RAMB` = Remote Access Mouse Button (см. выше). ✓
7. `Left2` / `Middle2` = двойной клик; `Left` / `Middle` = одиночный. ✓
8. `RightLeft` = right-затем-left. ✓
